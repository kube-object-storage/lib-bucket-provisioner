os: linux
dist: xenial
sudo: required
language: go
go:
  - 1.12.x
install:
  - curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
script:
  - make fmt fail-if-diff
  - make lint
  - make build
  - make test
